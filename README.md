# Lua Staging Area

## Installation

```
npm install
touch token
touch app.lua
```

Paste your FarmBot device token into the `token` file.
Paste your Lua code into `app.lua`.

## Run

```
node main.mjs
```
